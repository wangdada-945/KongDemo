{
  "name": "kong-manager-cypress-e2e",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "cypress:open": "cypress open",
    "cypress:run": "cypress run",
    "test": "npm run cypress:run",
    "test:ci": "cypress run"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "cypress": "^13.15.0",
    "cypress-multi-reporters": "^2.0.5",
    "mocha": "^10.8.2",
    "mocha-junit-reporter": "^2.2.1"
  }
}
