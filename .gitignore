node_modules
npm-debug.log*
yarn-debug.log*
yarn-error.log*

cypress/screenshots
cypress/videos
cypress/downloads
cypress/results

.DS_Store

# Local env vars (do not commit secrets)
.env
.env.*

# Pytest / logs — may contain tokens or credentials in HTML/log output
reports/*
!reports/.gitkeep

